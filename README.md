# 深圳＋香港五天四晚行程网站

本目录可直接上传到 GitHub 仓库根目录。

## GitHub Pages

1. 上传 `index.html` 到仓库根目录。
2. 打开 `Settings` → `Pages`。
3. Source 选择 `Deploy from a branch`。
4. 选择 `main` 和 `/(root)` 后保存。

四日交互地图使用 Leaflet 与 OpenStreetMap 在线底图，查看地图时需要联网。正文内容不依赖地图脚本。
